# --- Classical ML ---
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.10.0
matplotlib>=3.7.0
streamlit>=1.30.0
joblib>=1.3.0
PyYAML>=6.0.0

# --- Transformer (DistilBERT) ---
# NOTE: torch should match server CUDA version.
# Server already has PyTorch 2.5.1+cu121, skip if pre-installed.
# pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu121
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0
